const routes = {
    home: "/",
    watching: "/watching/:type/:id",
    filter: "/filter",
    ranking: "/ranking",
    following: "/following",
    setting: "/setting",
    // /authen
    // /welcome
};

export default routes;
