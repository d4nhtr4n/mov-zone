const routes = {
    home: "/",
    viewingTracking: "/view",
    viewing: "/view/:type/:id",
    filter: "/filter",
    ranking: "/ranking",
    following: "/following",
    setting: "/setting",
    // /authen
    // /welcome
};

export default routes;
