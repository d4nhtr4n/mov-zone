const routes = {
    home: "/",
    movie: "/movie/:id",
    filter: "/filter",
    ranking: "/ranking",
    following: "/following",
    setting: "/setting",
    // /authen
    // /welcome
};

export default routes;
